[287. Find the Duplicate Number](https://leetcode.com/problems/find-the-duplicate-number/) -> Uses floyd's algorithm 

# Easy
1. [[118. Pascal's Triangle]]
2. [[121. Best Time to Buy and Sell Stock]]
3. [[88. Merge Sorted Array]]
4. [[169. Majority Element]]
5. [[1. Two Sum]]
6. [[217. Contains Duplicate]]
7. [[242. Valid Anagram]]

# Medium
1. [[73. Set Matrix Zeroes]]
2. [[31. Next Permutation]]
3. [[53. Maximum Subarray]]
4. [[75. Sort Colors]]
5. [[48. Rotate Image]]
6. [[56. Merge Intervals]]
7. [[287. Finding the duplicate number]]
8. [[74. Search a 2D Matrix]]
9. [[50. Pow(x, n)]]
10. [[229. Majority Element II]]
11. [[62. Unique Paths]]
12. [[18. 4 Sum]]
13. [[128. Longest Consecutive Sequence]]
14. [[49. Group Anagrams]]
15. [[238. Product of Array Except Self]]
16. [[347. Top K Frequent Elements]]
17. [[36. Valid Sudoku]]
# Hard
1. [[493. Reverse Pairs]]
2. [[Find the repeating and missing number]]


## Notes

This is how you create a new array with elements. 
```java
int[] array = new int[]{i, map.get(comp)};
```

Sorting a string ([Refer](https://leetcode.com/problems/group-anagrams/submissions/1827461335/))
```java
char[] charArray = str.toCharArray();
Arrays.sort(charArray);
String key = String.valueOf(charArray);
```

Priority Queue, and how to use an array in a priority queue
```java
class Solution {
    public int[] topKFrequent(int[] nums, int k) {
        HashMap<Integer, Integer> map = new HashMap<>();
        for(int num : nums) map.put(num, map.getOrDefault(num, 0) + 1);
        PriorityQueue<int[]> heap = new PriorityQueue<>((a, b) -> Integer.compare(b[1], a[1]));

        for(Map.Entry<Integer, Integer> entry : map.entrySet()){
            heap.offer(new int[]{entry.getKey(), entry.getValue()});
        }

        int[] result = new int[k];
        while(--k >= 0){
            result[k] = (heap.poll())[0];
        }

        return result;
    }
}
```